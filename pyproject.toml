[project]
name = "olamundo"
version = "0.1.0"
description = "Projeto OlaMundo inicializado"
authors = ["Usuário <usuario@example.com>"]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
