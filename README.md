# Olá, Mundo!
Primeiro repositório versionado; Repositório criado atraves do curdo de git e github.

## Como usar

Requisitos: Python 3.8+

Executar o runner:

```
python -m src.main
# ou
python src/main.py --name "Seu Nome"
```

Exemplo:

```
python src/main.py --name "Ana"
```
